#include <bits/stdc++.h>
using namespace std;

int arr[100001];


int main() {
    int T, num;

    cin >> T;
    
    while(T--) {
        cin >> num;
        for(int i = 1; i <= num; i++) {
            cin >> arr[i];
        }
        
    }
}